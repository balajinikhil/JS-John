/*Rules
1.Can include letters, digits, underscore and dollar sign
but it must start only with letters, $ or  _ 

2.Must not include keywords

3.Case sensitive (firstname != firstName)

4.Camel Casing(firstName) or Underscore(first_name) format

5.let const var
*/

let name = `john`;

console.log(name);
